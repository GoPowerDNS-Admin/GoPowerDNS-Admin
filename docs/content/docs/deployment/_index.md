---
title: Deployment
description: "Deploy GoPowerDNS-Admin as a Docker container, standalone binary, or behind a reverse proxy, with built-in or upstream TLS."
weight: 4
---

GoPowerDNS-Admin can be deployed as a standalone binary, a Docker container, or behind a reverse proxy. All three modes support TLS termination — either by the application itself or upstream.
